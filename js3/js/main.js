/**
 * Strings
 */
const myVariable = "Mathematics";

/**
 * .length method - Shows the length of the variable. 
 */
console.log(myVariable.length);//Returns 11

/**
 * .split() method - separates the string into elements in an array, based on the character that should be in between them. In this case space or " "
 */
console.log("Every Good boy spells well".split(" "));//Returns an array

//Dot Notation master
