// Conditionals: If Statements

// Set variable soup
let soup;

// Set variable reply;
let reply;

if (soup) {
    reply = `Here's your order of ${soup}`;
} else {
    reply = "Sorry, we're out of soup.";
}

console.log(reply);